import type { IStorageAdapter } from '../../types/adapter.types.js';
/**
 * IStorageAdapter interface — re-exported from types for adapter implementors.
 * @see src/types/adapter.types.ts
 */
export type { IStorageAdapter };
/**
 * @deprecated Use `import type { IStorageAdapter } from '../types/adapter.types.js'` directly.
 * This file is kept for backwards compatibility.
 */
export declare const _adapterCompat = true;
