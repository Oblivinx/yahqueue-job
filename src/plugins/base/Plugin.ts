import type { IPlugin } from '../../types/plugin.types.js';

/**
 * IPlugin interface — re-exported for plugin implementors.
 * All plugins must implement this interface.
 */
export type { IPlugin };
